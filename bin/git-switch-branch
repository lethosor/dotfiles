#!/bin/bash

set -e
type fzf >/dev/null
git rev-parse HEAD >/dev/null
git recent-branches --limit=1 "$@" >/dev/null
branch="$(git recent-branches --limit=-1 "$@" | fzf --ansi --height 10 --preview 'git log -1 {-1}' | sed -E 's/.+?: //')"
test -n "$branch"
git switch "$branch"
