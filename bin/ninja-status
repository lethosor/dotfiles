#!/bin/sh

ninjas="$(pgrep -d, ninja)"
if test -n "$ninjas"; then
    children="$(pgrep -d, -P "$ninjas")"
    if test -n "$children"; then
        ps -o ppid,pid,command -p "$children" | cat
    fi
fi
